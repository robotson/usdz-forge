# Build artifacts
.build/
dist/
*.usdz

# Bundled engine runtime — fetched by engine/setup-engine.sh, never committed
engine/python/
engine/venv/
engine/vendor-cpython-*.tar.gz

# Reference clone (not part of the shippable app)
engine/usd_from_gltf/

# Python caches
__pycache__/
*.pyc
*.pyo

# Generated icon artifacts (produced from an icon master by make-icon.py)
packaging/AppIcon.iconset/
packaging/AppIcon.icns
packaging/icon_master_1024.png

# macOS
.DS_Store

# Private / internal notes — never ship
*PRIVATE*
*private*
